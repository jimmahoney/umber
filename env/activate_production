# env/activate_production
#
# To interact with the production database ,
# load this from the project root folder (i.e. ..) with
#
#     $ . env/acviate_production

# Define the project's environment variables.
source env/shell_production

# Activate the python virtual environment.
# (This assumes that ../venv/ has already been created
# and it's packages installed; see ../Makefile & ./install_notes.txt)
source venv/bin/activate

# Add the project python src folder to the python load path.
export PYTHONPATH=$PYTHONPATH:$UMBER_ROOT/src
