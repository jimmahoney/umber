# env/activate
#
# To define the python environment for a virtualenv python
# load this from * the project root folder * (i.e. umber/) with
#
#     $ . env/activate
#

# First define the project environment variables.
source env/set_shell_variables

# Then load the python environment and its software paths.
#   * This assumes that ../venv/ has already been created
#     and it's packages installed; see ../Makefile & ./install_notes.txt
source venv/bin/activate
