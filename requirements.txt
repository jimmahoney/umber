# -- requirements.txt --
#
# git >= 1.7
# sqlite3 >= 3.7
# virtualenv >= 1.9
# python >= 2.7

# You can install the remaining ones with
#
#  $ . env/bin/activate
#  (env)$ pip install -r requirements.txt

Flask >= 0.9
SQLAlchemy >= 0.8

# wiki, markupdown to html
python-creole >= 1.0
markdown >= 2.0

# syntax highligting
Pygments >= 1.6

# Flask-WTF >= 0.8

